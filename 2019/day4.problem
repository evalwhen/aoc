part2

这位精灵刚想起了一个非常关键的细节：那两个相邻且相同的数字，不能是更长重复数字串的一部分。

基于这个新标准（同时依然忽略数值范围限制），现在的情况如下：

    112233 符合要求：因为数字从不递减，且所有重复的数字都恰好只有两位长。

    123444 不再符合要求：因为重复的 44 实际上是一个更长的重复组 444 的一部分。

    111122 符合要求：虽然 1 重复了四次，但这个数字中仍然包含一个独立的双位组 22。
